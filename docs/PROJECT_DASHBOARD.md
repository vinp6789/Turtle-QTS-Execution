# PROJECT_DASHBOARD.md

> **DEPRECATED — superseded by [`PROJECT_STATE.md`](PROJECT_STATE.md).**
> This document formerly served as the "read this first" single-page
> summary, alongside `PROJECT_STATUS.md`'s longer-form version — the two
> roles have now been merged into one document to eliminate the drift
> that inevitably crept in between them. `PROJECT_STATE.md` is now the
> **single** authoritative execution-state document — read it first,
> always.
>
> **Retained for history, per `PROJECT_CONSTITUTION.md`'s decision-log
> discipline (never rewrite history).** Nothing below is current. Do not
> update this file — update `PROJECT_STATE.md` instead.

---

## Historical summary (as of the last time this file was current, 2026-07-28)

Overall completion toward the long-term vision was estimated at ~50–55%.
Five research campaigns had closed, all 18 experiments rejected, zero
approved alpha models. Execution Engine frozen and independently audited,
conditional GO for testnet pending 2 operator actions. Full regression:
1,602 passing.

For everything current — headline numbers, engineering status, research
status, operations status, risks, and where to go next — see
**[`PROJECT_STATE.md`](PROJECT_STATE.md)**.
