# PROJECT_STATUS.md

> **DEPRECATED — superseded by [`PROJECT_STATE.md`](PROJECT_STATE.md).**
> This document formerly split the project's execution-state role with
> `PROJECT_DASHBOARD.md`, which caused the two to drift out of sync with
> each other and with the actual repository (e.g. this file's own text
> once said "three campaigns complete" in one section and referenced five
> elsewhere). `PROJECT_STATE.md` is now the **single** authoritative
> execution-state document — read it first, always
> (`PROJECT_STATE.md`'s own "Read Order" section explains why).
>
> **Retained for history, per `PROJECT_CONSTITUTION.md`'s decision-log
> discipline (never rewrite history):** this file's git history holds the
> full point-in-time status snapshots written after Campaigns 01, 02, and
> 03 closed. Nothing below is current. Do not update this file — update
> `PROJECT_STATE.md` instead.

---

## Historical summary (as of the last time this file was current, 2026-07-28)

Two subsystems: Execution Engine (Modules 1–10 + app layer) frozen and
independently audited, conditional GO for testnet; Alpha Engine
(`alpha_engine/` + `research/`) complete and validated end-to-end across
five closed research campaigns, all rejected, zero approved alpha models.
Full regression at the time: 1,576+ tests passing.

For everything current — phase, completion estimate, active work,
backlog, blockers, risks, decision register, and the next executable
tasks — see **[`PROJECT_STATE.md`](PROJECT_STATE.md)**.
